#include <stdio.h>
#include <string.h>
struct student
{
    char name[10];
    int year;
    struct student *next;
};
typedef struct student STU;

int main(void)
{
    int *head=NULL;
    int *next=NULL;
    p=head=malloc(sizeof(STU));
    if()
    {
        perror(create);
        exit(0);
    }
    
    p -> next = NULL;
    for (i = 0; i < n; i++)
    {
         
    
    }
}
